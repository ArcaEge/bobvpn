# bobvpn

Simple VPN protocol written in rust, made to get around restricted networks that only allow outbound TCP 443 connections (like a school network)

It's entirely vibecoded so no guarantees on quality
